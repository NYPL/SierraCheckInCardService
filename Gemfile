source 'https://rubygems.org'

gem 'aws-sdk-s3'
gem 'nypl_ruby_util'

group :deployment do
    gem 'aws-sdk-cloudwatch'
    gem 'aws-sdk-lambda'
end

group :test do
    gem 'mocha'
    gem 'rspec'
    gem 'rubocop', require: false
    gem 'sqlite3'
end
